use crate::model::board::Board;
use crate::model::stateless::{Intersection, Road};

pub fn generate_roads(_board: &mut Board<Option<Intersection>, Option<Road>>) {
    // TODO generate roads randomly
    unimplemented!()
}
