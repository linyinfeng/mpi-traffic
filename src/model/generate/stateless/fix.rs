use crate::model::board::Board;
use crate::model::stateless::{Intersection, Road};

pub fn fix(_board: &mut Board<Option<Intersection>, Option<Road>>) {
    // TODO fix conflict in board data
    unimplemented!()
}
